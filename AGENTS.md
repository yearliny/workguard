# WorkGuard 开发约束

- 产品名“工作防沉迷”，代码名 WorkGuard，仓库建议名 workguard。
- Windows 10 22H2 / Windows 11 x64 为当前目标，C# + .NET 10 + WPF。Core / Data 不得依赖 WPF 或 Windows API。
- 不引入后台服务、账号、遥测、广告、云依赖；健康相关数据本地保存。
- 时间推进必须可测试，核心不得直接读取系统时间。连续计时使用单调时钟；日统计使用本地日期。
- 静默提醒与已完成休息是不同概念。会议 / 演示不得直接重置健康暴露计时。
- 无键鼠输入只能推测空闲。任何新算法都要写明误判边界，不宣称识别身体姿势。
- 不监听具体按键，不截屏，不采集窗口标题，不使用摄像头或麦克风。
- 退出始终可用，不拦截系统快捷键，不实现不可逃离的覆盖层。
- 跳过、不足时长、睡眠跨越不应获得完整活动计数。
- 日统计和设置变更要考虑写入失败、版本冲突和文件损坏，避免破坏用户已有数据。
- 修改计时决策、状态持久化或动作完成语义时增加行为测试；普通文案修改不必新增测试。
- 验证：`dotnet run --project tests/WorkGuard.Tests -c Release`；`dotnet build WorkGuard.slnx -c Release`；Windows 上 `./scripts/smoke-windows.ps1`。
- WPF 不能在 Linux 实际运行。跨平台编译通过不能表述为 Windows 实机验收完成。
- 不增加缺乏证据的治疗、预防、健康评分或卡路里承诺。新增动作先说明适用边界。
