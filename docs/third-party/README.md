# 第三方运行时

便携版包含 Microsoft .NET 与 Windows Desktop 运行时，保留随运行时分发的许可证和第三方声明。

本次文件取自用于交付构建的 10.0.11 win-x64 官方 NuGet 运行时包。更新运行时版本或构建其他架构时，应从对应官方包核对并更新声明，然后再分发。

这些许可证仅适用于对应第三方组件，不自动为 WorkGuard 项目本身授予开源许可证。
